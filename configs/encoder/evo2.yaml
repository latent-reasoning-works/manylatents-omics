# Evo2 DNA Encoder
# Model: evo2_1b_base (1B params)

_target_: manylatents.dogma.encoders.Evo2Encoder
model_name: evo2_1b_base
layer_name: null  # Uses default middle layer
device: cuda

# Metadata for config composition
modality: dna
embedding_dim: 2048
