# Orthrus RNA Encoder (4-track)
# Model: orthrus_base_4_track

_target_: manylatents.dogma.encoders.OrthrusEncoder
n_tracks: 4
run_path: null  # Uses default path
checkpoint: null  # Uses default checkpoint
device: cuda

# Metadata for config composition
modality: rna
embedding_dim: 256
